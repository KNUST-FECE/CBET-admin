import { RefreshCw } from "lucide-react";

export default function RunFilter() {
    return (
        <button id="filter-run">
            <RefreshCw />
            <span>run</span>
        </button>
    )
}